version: '3.4'

services:
  getoffers:
    image: getoffers
    build:
      context: .
      dockerfile: ./Dockerfile
    command: ["sh", "-c", "pip install debugpy -t /tmp && python /tmp/debugpy --wait-for-client --listen 0.0.0.0:5678 游戏机跟卖2\update.py "]
    ports:
      - 5678:5678
